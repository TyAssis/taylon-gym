const { assert } = require('chai');
const { gupyPricing } = require('../gupy-pricing/gupyPricingRefactored.js');

describe('initial test gupyPricing', () => {
    it('', () => {
		assert.equal(gupyPricing(), '');
	});
});