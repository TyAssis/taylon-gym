name: Test Environment Variable

on:
  workflow_dispatch:
  push:
    branches:
    - test

env:
  test: ${{ vars.TEST }}

jobs:
  getEnvVar:
    name: Get environment variable
    runs-on: ubuntu-latest
    steps:
      - name: get var
        run: echo "$test"
